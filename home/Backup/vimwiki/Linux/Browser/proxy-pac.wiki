    = proxy =
%toc
== proxy.pac ==
{{{
    function FindProxyForURL(url,host){
	var NoProxy = "DIRECT";
	var GoProxy = "PROXY 127.0.0.1:8087";

	var WHITELIST = new Array(
		".ip138.com",
		".google.com");

	for (i in WHITELIST) {
	    if(shExpMatch(url.toLowerCase(),"*" + WHITELIST[i].toLowerCase() + "*")){return GoPrxy;};
	};
	return "DIRECT";
    };
  }}}
